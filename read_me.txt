download our project files 'scatter.py' and 'map.py. to a folder on you pc.

open anaconda prompt and change the directory in you anaconda prompt to the directory where you have downloaded our files

firs run:

bokhe serve --show scatter.py


afterwards run:

bokeh serve --show maps.py